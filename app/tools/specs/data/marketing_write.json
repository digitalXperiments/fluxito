[
  {
    "tool": "marketing_write",
    "action": "create_campaign",
    "summary": "Create a new campaign. Google requires channel type + budget + start; Meta reuses advertising_channel_type as the objective. Irreversible (creates a live ad object).",
    "group": "PAID ADS (Google / Meta / TikTok / Snap)",
    "params": [
      {
        "name": "platform",
        "type": "string",
        "required": true,
        "enum": [
          "google",
          "meta"
        ],
        "example": "google",
        "doc": "Only google and meta implement create_campaign. tiktok/snap/etc. return 'Unknown action ... write'."
      },
      {
        "name": "account_id",
        "type": "string",
        "required": true,
        "example": "123-456-7890",
        "doc": "Ad account / customer id (validated up front)."
      },
      {
        "name": "campaign_name",
        "type": "string",
        "required": true,
        "example": "Spring Sale",
        "doc": "Campaign name. Required for both google and meta."
      },
      {
        "name": "advertising_channel_type",
        "type": "string",
        "required": true,
        "enum": [
          "SEARCH",
          "DISPLAY",
          "SHOPPING",
          "VIDEO",
          "PERFORMANCE_MAX"
        ],
        "example": "SEARCH",
        "doc": "Google: channel type. Meta: reused as the campaign OBJECTIVE (e.g. OUTCOME_SALES, OUTCOME_TRAFFIC) \u2014 same field, totally different value space."
      },
      {
        "name": "daily_budget_usd",
        "type": "number",
        "required": true,
        "example": 100.0,
        "doc": "Required for Google (\u2192 micros). For Meta it is optional (passed through as daily_budget, may be None).",
        "platforms": [
          "google",
          "meta"
        ]
      },
      {
        "name": "start_date",
        "type": "string",
        "required": true,
        "example": "2026-06-01",
        "doc": "YYYY-MM-DD. Required for Google only; not in Meta's required list.",
        "platforms": [
          "google"
        ]
      },
      {
        "name": "end_date",
        "type": "string",
        "required": false,
        "example": "2026-06-30",
        "doc": "Google: optional end date."
      },
      {
        "name": "bidding_strategy_type",
        "type": "string",
        "required": false,
        "example": "MAXIMIZE_CLICKS",
        "doc": "Google: defaults to MAXIMIZE_CLICKS when omitted."
      },
      {
        "name": "status",
        "type": "string",
        "required": false,
        "enum": [
          "ACTIVE",
          "PAUSED"
        ],
        "example": "PAUSED",
        "doc": "Meta only: defaults to PAUSED (uppercased)."
      }
    ],
    "platforms": [
      "google",
      "meta"
    ],
    "returns": "Connector result (created campaign id) | {error,...}",
    "scope": "Google: inline SCOPE_GA4_FULL (Full Access) check; Meta: token presence only",
    "reversible": false
  },
  {
    "tool": "marketing_write",
    "action": "marketo_add_to_list",
    "summary": "Adobe Marketo: add lead ids to a static list.",
    "group": "ADOBE MARKETO ENGAGE",
    "params": [
      {
        "name": "resource_id",
        "type": "string",
        "required": true,
        "example": "55",
        "doc": "Marketo list id."
      },
      {
        "name": "payload",
        "type": "object",
        "required": true,
        "example": {
          "lead_ids": [
            "1001",
            "1002"
          ]
        },
        "doc": "payload.lead_ids (array of strings) REQUIRED."
      }
    ],
    "platforms": [
      "marketo"
    ],
    "returns": "Raw Marketo REST envelope { result:[{id, status}] } | error",
    "reversible": true
  },
  {
    "tool": "marketing_write",
    "action": "marketo_remove_from_list",
    "summary": "Adobe Marketo: remove lead ids from a static list.",
    "group": "ADOBE MARKETO ENGAGE",
    "params": [
      {
        "name": "resource_id",
        "type": "string",
        "required": true,
        "example": "55",
        "doc": "Marketo list id."
      },
      {
        "name": "payload",
        "type": "object",
        "required": true,
        "example": {
          "lead_ids": [
            "1001",
            "1002"
          ]
        },
        "doc": "payload.lead_ids (array of strings) REQUIRED."
      }
    ],
    "platforms": [
      "marketo"
    ],
    "returns": "Raw Marketo REST envelope { result:[{id, status}] } | error",
    "reversible": true
  },
  {
    "tool": "marketing_write",
    "action": "marketo_request_campaign",
    "summary": "Adobe Marketo: trigger a smart campaign to run against specified leads (with optional my.tokens).",
    "group": "ADOBE MARKETO ENGAGE",
    "params": [
      {
        "name": "resource_id",
        "type": "string",
        "required": true,
        "example": "3001",
        "doc": "Marketo campaign id."
      },
      {
        "name": "payload",
        "type": "object",
        "required": true,
        "example": {
          "lead_ids": [
            "1001"
          ],
          "tokens": [
            {
              "name": "{{my.x}}",
              "value": "v"
            }
          ]
        },
        "doc": "payload.lead_ids (array) REQUIRED; payload.tokens (array of objects) optional."
      }
    ],
    "platforms": [
      "marketo"
    ],
    "returns": "Raw Marketo REST envelope | error",
    "reversible": false
  },
  {
    "tool": "marketing_write",
    "action": "marketo_schedule_campaign",
    "summary": "Adobe Marketo: schedule a batch campaign to run at a time (with optional tokens).",
    "group": "ADOBE MARKETO ENGAGE",
    "params": [
      {
        "name": "resource_id",
        "type": "string",
        "required": true,
        "example": "3001",
        "doc": "Marketo campaign id."
      },
      {
        "name": "payload",
        "type": "object",
        "required": false,
        "example": {
          "run_at": "2026-07-01T09:00:00Z",
          "tokens": [
            {
              "name": "{{my.x}}",
              "value": "v"
            }
          ]
        },
        "doc": "payload.run_at (ISO datetime) optional, payload.tokens (array) optional."
      }
    ],
    "platforms": [
      "marketo"
    ],
    "returns": "Raw Marketo REST envelope | error",
    "reversible": false
  },
  {
    "tool": "marketing_write",
    "action": "marketo_upsert_leads",
    "summary": "Adobe Marketo: POST /rest/v1/leads.json \u2014 create or update leads in bulk.",
    "group": "ADOBE MARKETO ENGAGE",
    "params": [
      {
        "name": "payload",
        "type": "object",
        "required": true,
        "example": {
          "leads": [
            {
              "email": "a@b.com",
              "firstName": "A"
            }
          ],
          "lookup_field": "email",
          "action": "createOrUpdate"
        },
        "doc": "payload.leads (array of objects) is REQUIRED. payload.lookup_field (default 'email'), payload.action (default 'createOrUpdate')."
      }
    ],
    "platforms": [
      "marketo"
    ],
    "returns": "Raw Marketo REST envelope { result:[{id, status}] } | error",
    "reversible": false
  },
  {
    "tool": "marketing_write",
    "action": "update_campaign_budget",
    "summary": "Change a campaign's daily budget. Mutates live ad spend. Google converts USD\u2192micros; Snap\u2192micros; TikTok passes USD as-is.",
    "group": "PAID ADS (Google / Meta / TikTok / Snap)",
    "params": [
      {
        "name": "platform",
        "type": "string",
        "required": true,
        "enum": [
          "google",
          "tiktok",
          "snap",
          "linkedin",
          "pinterest",
          "reddit"
        ],
        "example": "google",
        "doc": "Platforms whose write handler implements update_campaign_budget. meta/x/apple do NOT support it."
      },
      {
        "name": "account_id",
        "type": "string",
        "required": true,
        "example": "123-456-7890",
        "doc": "Ad account id. Validated up front for all non-marketo platforms (returns bad_request before action check)."
      },
      {
        "name": "campaign_id",
        "type": "string",
        "required": true,
        "example": "987654321",
        "doc": "Campaign to modify."
      },
      {
        "name": "daily_budget_usd",
        "type": "number",
        "required": true,
        "example": 50.0,
        "doc": "New daily budget in USD. Google/Snap multiply by 1_000_000 (micros); TikTok uses raw USD."
      }
    ],
    "platforms": [
      "google",
      "tiktok",
      "snap"
    ],
    "returns": "Connector result (updated budget) | {error,...}",
    "scope": "Google: inline SCOPE_GA4_FULL (Full Access) check; other platforms: none beyond token presence",
    "reversible": false
  },
  {
    "tool": "marketing_write",
    "action": "update_campaign_status",
    "summary": "Enable/pause a campaign. Mutates live delivery. status is uppercased before the connector call (google/meta/tiktok/snap); linkedin/pinterest/x/reddit/apple pass status as-is.",
    "group": "PAID ADS (Google / Meta / TikTok / Snap)",
    "params": [
      {
        "name": "platform",
        "type": "string",
        "required": true,
        "enum": [
          "google",
          "meta",
          "tiktok",
          "snap",
          "linkedin",
          "pinterest",
          "x",
          "reddit",
          "apple"
        ],
        "example": "google",
        "doc": "Supported on all 9 ad platforms."
      },
      {
        "name": "account_id",
        "type": "string",
        "required": true,
        "example": "123-456-7890",
        "doc": "Ad account id (validated up front)."
      },
      {
        "name": "campaign_id",
        "type": "string",
        "required": true,
        "example": "987654321",
        "doc": "Campaign to toggle."
      },
      {
        "name": "status",
        "type": "string",
        "required": true,
        "enum": [
          "ENABLED",
          "PAUSED"
        ],
        "example": "PAUSED",
        "doc": "Target state. google/meta/tiktok/snap call .upper() on it; other platforms pass it through verbatim (case-sensitivity inconsistency)."
      }
    ],
    "platforms": [
      "google",
      "meta",
      "tiktok",
      "snap",
      "linkedin",
      "pinterest",
      "x",
      "reddit",
      "apple"
    ],
    "returns": "Connector result (updated status) | {error,...}",
    "scope": "Google: inline SCOPE_GA4_FULL (Full Access) check; others: token presence only",
    "reversible": false
  }
]
